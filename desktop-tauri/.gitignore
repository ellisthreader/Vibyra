node_modules
dist
src-tauri/target
src-tauri/gen
*.log

# Packaged output and its checksums are release artifacts, not source.
release

# Local toolchain download used by scripts/setup-linux.sh.
node-v*-linux-x64.tar.xz

# Signed CI artifacts pulled down for a publish. Never commit these:
# one release set is ~119 MB and this repo is public.
release-set/
