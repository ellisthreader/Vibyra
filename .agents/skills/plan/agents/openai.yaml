interface:
  display_name: "Plan"
  short_description: "Plan, review, implement, verify"
  default_prompt: "Use Plan to turn a broad request into a reviewed implementation plan, execute it, verify it, and update durable notes."
