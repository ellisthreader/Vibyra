[phases.setup]
nixPkgs = ["php83", "php83Packages.composer", "nodejs_20"]

[phases.install]
cmds = ["composer install --no-dev --optimize-autoloader --no-interaction", "npm install -g @railway/cli"]

[phases.build]
cmds = ["php artisan config:clear", "php artisan route:clear"]

[start]
cmd = "php artisan config:clear && php artisan route:clear && php artisan migrate --force && mkdir -p storage/logs && (php artisan schedule:work >> storage/logs/scheduler.log 2>&1 &) && php artisan serve --host=0.0.0.0 --port=$PORT"
