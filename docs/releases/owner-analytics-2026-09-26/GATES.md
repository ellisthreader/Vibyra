# Owner analytics release gates — 26 September 2026

The local dashboard at `http://127.0.0.1:8128/owner/login` uses a real, timestamped aggregate production snapshot. Website, Desktop, and iOS product events are still unavailable in production. The candidate source archive has **not** been deployed.

## What is ready

- Latest website: server-held choice, named event allowlists, owner pages, operational token-use/cloud activity, privacy notice, Google-owner Vibyra TOTP enrollment, and a 15-minute aggregate snapshot refresh. The latest website agent reported 464 passed/1 skipped/3,201 assertions before its final provider UI branch, then focused owner tests 6/62 and Vite build passed. The integrated candidate below was validated after all final overlays.
- Production backend candidate: current running-image base plus 114 reviewed analytics/owner/CI overlays, hash inventory, isolated PHP suite 910 tests/6,454 assertions with one model mismatch, UI source suite 8/8, route cache and final Vite build passed. The dedicated owner enrollment test covers subject/account and session mismatch, expiry, replay, cancellation, local identity denial, public status rejection, and the independent TOTP step-up. CI generates an ephemeral test key; Gitleaks has exact historical false-positive fingerprints. Expo dependency alignment passes after a separate mobile patch. See `release-manifest.json` and `CI_FOLLOWUP.md` for provenance.
- Desktop 0.8.11: clean source commit and signed arm64 candidate in `Vibyra/output/desktop-analytics-0.8.11-release-candidate/`; full release verification passed. It is not notarized or installed.
- iOS 1.0.0 (2): signed local IPA in `~/Desktop/Vibyra-analytics-release-candidate/`; no TestFlight upload. The working tree is not a clean release baseline.

## Automated blockers

- A complete isolated candidate PHP run leaves **1 failure** across 910 tests and 6,454 assertions: the current mobile model picker contains models absent from the unchanged production backend shortlist, profiles, and billing configuration. Three packaging-context failures disappeared with root/mobile fixtures and an isolated home directory. Connector test-only corrections removed an unadvertised GitHub case and added real Google Tasks and HackerNews operation coverage; all 64 focused connector tests pass. A separate optional model parity branch is being reviewed because it affects Auto routing, pricing, and trial eligibility. It is not in this analytics archive or commit.
- The refreshed production security audit has **53 pass, 24 fail, 9 manual**. Six failures concern active mobile release architecture: Android LAN cleartext transport, absent `expo-updates` runtime/channel, local Xcode build numbering versus EAS remote versioning, and unconfigured verified iOS/Android links. Do not silence them with values that do not describe the shipped app; implement the architecture or obtain an independently reviewed gate change.
- The other 18 failures concern production service flags and missing credentials. Review the current service behavior before setting explicit CORS, legacy-route, moderation, LAN, and publishing values. Stripe, Apple IAP, Google IAP, and Google auth credentials are absent; decide the production feature scope and provision real credentials or independently revise the gate. The audit JSON names each failed check without containing secret values.
- A later read-only environment review also found `PUBLISH_REVIEW_FORCE_APPROVE_UNDER_REVIEW` effectively enabled in production. It can turn an under-review project into public approval. The new root audit check fails unless the flag is explicitly false; it is **not yet included** in the 53/24/9 audit JSON attached here. Do not change the value without reviewing the product behavior and an approved release plan.

## Manual and operational blockers

The repository's `docs/security/production-release-gates.md` requires passing automated checks plus dated evidence and an approver independent of the implementer. That includes protected commit provenance, signed/notarized artifacts, backup and restore, rollback, monitoring, incident response, device acceptance, and pentest evidence. No such release approval has been claimed here.

Read-only GitHub API inspection on 2026-09-26 found no repository rulesets and no branch protection on `main`. There is no environment named exactly `production`; the existing `spectacular-charisma / production` environment has no protection rules or deployment-branch policy. An administrator must configure the protected branch/environment and independent reviewers, then provide dated evidence. These observations do not satisfy the manual gates.
The latest visible Security CI run on `main` failed (2026-09-23); CodeQL succeeded on that older commit, and no Production Security Gate run is visible. The candidate is not tied to a protected, checked commit. Rerun all required checks on the eventual release commit.

The verified intended owner account is Google-provider and has Vibyra TOTP disabled. Released Desktop/iOS account settings cannot enroll provider accounts. This candidate adds **website-only** enrollment behind the configured, verified owner web session: an OAuth identity round trip must return the exact account's Google subject; a short-lived proof is bound to that web session and consumed once for TOTP setup. The Google callback does not create a new app session. Google may reuse an existing Google session during the round trip, so this is subject confirmation, not guaranteed fresh Google authentication or Google MFA. Named Accounts still requires confirmed Vibyra TOTP and an independent fresh owner step-up. `VIBYRA_OWNER_EMAILS` is unset; configure the supplied verified account only during an approved release. Do not put that email in client code or a test login. The local one-click owner remains aggregate-only.

GeoLite2 credentials and a production country database are absent, so country breakdowns remain unknown until provisioned. No historical surface events can be recovered. After an approved backend release, verify migration, consent rejection and withdrawal, live accepted events, the owner gate, country coverage, and snapshot freshness before distributing native clients.

The local review branch `codex/owner-analytics-production-review-20260926` begins with `30453116` (exact running backend source) and `8d85ad30` (analytics/owner overlays), followed by review evidence and separate CI/Expo fixes. The latest CI/test-fixture and Expo commits are local pending review. It is **not** a protected release commit. Compare `release-manifest.json` and `DRAFT_PR_BODY.md`. The old `main` cannot be treated as the deployed source. A protected branch/environment, independent review, fresh CI, production image recheck, and manual evidence remain necessary.
