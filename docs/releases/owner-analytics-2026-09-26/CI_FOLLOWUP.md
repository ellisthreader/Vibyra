# Draft PR CI follow-up — 26 September 2026

This note records sanitized causes from draft PRs #28 and #29. It does not change production behavior or release approval.

| Check | Observed cause | Disposition |
| --- | --- | --- |
| #28 Secret history scan | Three `generic-api-key` detections in baseline commit `30453116`: `backend/tests/Feature/SpeechEndpointTest.php:17` uses a synthetic OpenAI test key; `backend/tests/Feature/RemoteAccessTest.php:21` uses a fixed signing test fixture; `backend/public/phone/_expo/static/js/web/index-eb4d87d19fdf5a9b44f38fd78f24c42d.js:854` contains bundled `FourKeyMap` class code. No actual provider credential was identified. | `.gitleaksignore` lists exactly those three finding fingerprints. Gitleaks 8.24.3 with the action's commit range finds three before the ignore and zero after it. #29's relative scan passed because its base is #28. Do not add path-wide exclusions. |
| #28/#29 Backend checks | 347 feature failures are `MissingAppKeyException`; neither existing workflow nor `phpunit.xml` supplies a test encryption key. The backend CI job on old `main` also failed. | Generate a random 32-byte `APP_KEY` for the CI job through `$GITHUB_ENV`, without logging or committing it. Run PHPUnit directly with PHP `memory_limit=512M`; `php artisan test` launches a child that does not inherit the CLI `-d` setting. The larger test limit is needed by an existing screenshot test. The current mobile picker is absent from the old checked-in mobile tree, so a checked-in 24-ID fixture makes the actual shortlist mismatch visible in #28; #29's coordinated model changes should clear that mismatch. |
| #28 Backend checks after CI fixes | The job reached 910 PHPUnit cases and failed `ProductionProcessTopologyTest::test_active_platform_configs_use_the_role_aware_launcher` because the branch still inherited `origin/main`'s root `railway.json` command that starts only `php artisan serve`. The separate `VibesAutoGuardrailsTest` shortlist mismatch is the expected model gate. | Change only the root `railway.json` start command to `cd backend && bash scripts/start-production.sh`, matching the already present launcher and current local main checkout. Focused topology validation passes 3 tests/10 assertions. The reviewed Railway service uses `/backend` as root, so this repo-root correction does not alter the backend source archive. |
| #28 Mobile checks | `npx expo install --check` rejects eight Expo 57 packages below its current expected patch versions. The checked-in mobile dependency files match old `main`; this is version drift, not an analytics edit. | A separate, clean native dependency update is under review. Run install/check/export and the complete mobile job after integration. |
| #28/#29 Dependency Review | The action exits before comparing packages: “Dependency review is not supported on this repository.” The repository is public and #28 targets default `main`; the dependency-graph SBOM API returned 404. | GitHub's public-repository documentation says the graph is normally enabled. An administrator should inspect Settings → Security and quality → Advanced Security and contact GitHub support if the graph appears enabled yet the API/action remain unavailable. Do not treat this as a vulnerability result or silence the check without an independently reviewed gate decision. |

This CI patch is separate from the owner analytics runtime source. It does not change the running Railway image, production variables, or native distribution.
